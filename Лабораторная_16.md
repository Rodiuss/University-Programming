# Задача 1

> Построить изображение, приведенное на рисунке, применив како-нибудь способ раскраски.

```pascal
program task1;
uses GraphABC;
begin
  SetWindowSize(500,500);
  
  //SetBrushStyle(BrushStyleType.bsGradient);
  //(Brush.NETBrush as LinearGradientBrush).LinearColors := new Color[2](clBlue, clRed);
  FillRect(100, 100, 200, 200);
  for var i := 1 to 5 do
  begin
    line(80, i * 80, 400, i * 80);
    line(80 * i, 80, 80 * i, 400);
  end;
  
  for var i := 1 to 5 do
  begin
    line(80, i * 80, 400, i * 80);
    line(80 * i, 80, 80 * i, 400);
  end;
  line(80, 320, 320, 80);
  line(160, 400, 400, 160);
  line(80, 320, 160, 400);
  line(320, 80, 400, 160);
  line(160, 80, 80, 160);
  line(320, 400, 400, 320);
  line(80, 160, 320, 400);
  line(160, 80, 400, 320);
end.
```

# Задача 2

> Построить график функции y = cos(x – 1) + |x| на отрезке [-2π;3π] (* см.Справочный материал).

```pascal
program Lab16_Task2;

uses
  GraphABC, System;

const
  WINDOW_WIDTH = 800;
  WINDOW_HEIGHT = 600;
  A = -2 * Pi;  // Начало отрезка
  B = 3 * Pi;   // Конец отрезка

function F(x: real): real;
begin
  Result := Cos(x - 1) + Abs(x);
end;

procedure DrawGraph;
var
  x0, y0, scaleX, scaleY: integer;
  x, y: real;
  px, py: integer;
  i: integer;
begin
  x0 := WINDOW_WIDTH div 2;
  y0 := WINDOW_HEIGHT div 2;
  scaleX := 50;  // Масштаб по X
  scaleY := 50;  // Масштаб по Y
  
  Pen.Color := clBlack;
  Pen.Width := 2;
  
  Line(0, y0, WINDOW_WIDTH, y0);
  Line(x0, 0, x0, WINDOW_HEIGHT);
  
  Line(WINDOW_WIDTH, y0, WINDOW_WIDTH - 10, y0 - 5);
  Line(WINDOW_WIDTH, y0, WINDOW_WIDTH - 10, y0 + 5);
  Line(x0, 0, x0 - 5, 10);
  Line(x0, 0, x0 + 5, 10);
  
  TextOut(WINDOW_WIDTH - 15, y0 - 20, 'X');
  TextOut(x0 + 10, 5, 'Y');
  
  Pen.Color := clGray;
  Pen.Width := 1;
  
  for i := Trunc(A) to Trunc(B) do
  begin
    px := x0 + Round(i * scaleX);
    Line(px, y0 - 5, px, y0 + 5);
    if i <> 0 then
      TextOut(px - 5, y0 + 10, IntToStr(i));
  end;
  
  for i := -10 to 10 do
  begin
    py := y0 - Round(i * scaleY);
    Line(x0 - 5, py, x0 + 5, py);
    if i <> 0 then
      TextOut(x0 + 10, py - 8, IntToStr(i));
  end;
  
  Pen.Color := clBlue;
  Pen.Width := 3;
  
  x := A;
  y := F(x);
  px := x0 + Round(x * scaleX);
  py := y0 - Round(y * scaleY);
  MoveTo(px, py);
  
  while x <= B do
  begin
    y := F(x);
    px := x0 + Round(x * scaleX);
    py := y0 - Round(y * scaleY);
    
    if (px >= 0) and (px <= WINDOW_WIDTH) and 
       (py >= 0) and (py <= WINDOW_HEIGHT) then
      LineTo(px, py)
    else
      MoveTo(px, py);
    
    x := x + 0.01;
  end;
  
  SetFontColor(clBlue);
  SetFontSize(12);
  TextOut(10, 10, 'y = cos(x - 1) + |x|');
  TextOut(10, 30, 'Отрезок: [-2π; 3π]');
end;

begin
  SetWindowSize(WINDOW_WIDTH, WINDOW_HEIGHT);
  SetWindowTitle('Лабораторная работа 16 - Задание 2');
  ClearWindow(clWhite);
  
  DrawGraph;
end.
```

# Задача 3

> Построить окружность с двумя взаимно-перпендикулярными диаметрами, если заданы центр и радиус окружности.

```pascal
program Lab16_Task3;

uses
  GraphABC;

const
  WINDOW_WIDTH = 600;
  WINDOW_HEIGHT = 400;

procedure DrawCircleWithDiameters;
var
  centerX, centerY, radius: integer;
begin
  centerX := WINDOW_WIDTH div 2;
  centerY := WINDOW_HEIGHT div 2;
  radius := 150;
  
  Pen.Color := clBlue;
  Pen.Width := 3;
  Brush.Color := clTransparent;
  Circle(centerX, centerY, radius);
  
  Pen.Color := clRed;
  Pen.Width := 2;
  Line(centerX - radius, centerY, centerX + radius, centerY);
  
  Pen.Color := clGreen;
  Line(centerX, centerY - radius, centerX, centerY + radius);
  
  Pen.Color := clBlack;
  Pen.Width := 5;
  SetPixel(centerX, centerY, clBlack);
  Circle(centerX, centerY, 3);
  
  SetFontColor(clBlue);
  SetFontSize(12);
  
  SetFontColor(clBlack);
  TextOut(10, 10, 'Центр: (' + IntToStr(centerX) + ', ' + IntToStr(centerY) + ')');
  TextOut(10, 30, 'Радиус: ' + IntToStr(radius));
end;

begin
  SetWindowSize(WINDOW_WIDTH, WINDOW_HEIGHT);
  SetWindowTitle('Лабораторная работа 16 - Задание 3');
  ClearWindow(clWhite);
  
  DrawCircleWithDiameters;
end.
```

# Задача 4

> Построить концентрические окружности одного цвета, если заданы координаты общего центра (х,у), количество окружностей – N, радиус наименьшей окружности – R и шаг изменения по радиусу St. 

```pascal
program Lab16_Task4;

uses
  GraphABC;

const
  WINDOW_WIDTH = 600;
  WINDOW_HEIGHT = 400;

procedure DrawConcentricCircles;
var
  centerX, centerY: integer;
  N, R, St, i: integer;
  currentRadius: integer;
begin
  writeln('Введите параметры концентрических окружностей:');
  
  write('Координата X центра: ');
  readln(centerX);
  
  write('Координата Y центра: ');
  readln(centerY);
  
  write('Количество окружностей (N): ');
  readln(N);
  
  write('Радиус наименьшей окружности (R): ');
  readln(R);
  
  write('Шаг изменения радиуса (St): ');
  readln(St);
  
  SetWindowSize(WINDOW_WIDTH, WINDOW_HEIGHT);
  SetWindowTitle('Лабораторная работа 16 - Задание 4');
  ClearWindow(clWhite);
  
  if (centerX < 0) or (centerX > WINDOW_WIDTH) or
     (centerY < 0) or (centerY > WINDOW_HEIGHT) then
  begin
    centerX := WINDOW_WIDTH div 2;
    centerY := WINDOW_HEIGHT div 2;
    writeln('Центр установлен в середину окна: (', centerX, ', ', centerY, ')');
  end;
  
  currentRadius := R;
  
  for i := 1 to N do
  begin
    Pen.Color := RGB(100, 100, 200 + i * 5);
    Pen.Width := 2;
    Brush.Color := clTransparent;
    
    Circle(centerX, centerY, currentRadius);
    
    currentRadius := currentRadius + St;
    
    if currentRadius > Min(centerX, centerY) then
      break;
  end;
  
  Pen.Color := clRed;
  Pen.Width := 5;
  SetPixel(centerX, centerY, clRed);
  Circle(centerX, centerY, 5);
  
  SetFontColor(clBlack);
  SetFontSize(11);
  TextOut(10, 10, 'Концентрические окружности');
  TextOut(10, 30, 'Центр: (' + IntToStr(centerX) + ', ' + IntToStr(centerY) + ')');
  TextOut(10, 50, 'Количество: ' + IntToStr(N));
  TextOut(10, 70, 'Начальный радиус: ' + IntToStr(R));
  TextOut(10, 90, 'Шаг радиуса: ' + IntToStr(St));
  
  TextOut(centerX + 10, centerY - 10, 'Центр');
end;

begin
  DrawConcentricCircles;
end.
```
